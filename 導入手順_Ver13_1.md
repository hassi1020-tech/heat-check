# Ver.13.1 Googleログイン安定版 導入手順

この版では、Googleログインをポップアップ方式から
「画面遷移型のリダイレクト認証」へ変更しています。

## 1. GitHubへ上書き

ZIPを展開し、`hassi1020-tech/heat-check` の直下へ
全ファイルをドラッグして上書きしてください。

特に次のファイルは必ず更新してください。

- index.html
- firebase-cloud.js
- firebase-config.js
- sw.js
- manifest.webmanifest
- styles.css

## 2. GitHub Pages反映後のURL

PC管理画面:

```text
https://hassi1020-tech.github.io/heat-check/?mode=admin&v=1310auth
```

モバイル測定画面:

```text
https://hassi1020-tech.github.io/heat-check/?mode=kiosk&v=1310auth
```

## 3. 初回は古いキャッシュを削除

Chrome・Edge:

1. 対象ページを開く
2. F12
3. Application
4. Storage
5. Clear site data
6. ブラウザを閉じて再度開く

簡易操作では `Ctrl + Shift + R` でも更新できます。

## 4. Googleログインの動き

「Googleログイン」を押すと、ポップアップではなく
Googleのログイン画面へ移動します。

認証後は自動的にアプリへ戻り、
Firebase同期欄が「同期済み」になります。

## 5. Firebase側の必須設定

Firebase Console:

Authentication
→ Sign-in method
→ Google
→ 有効

Authentication
→ 設定
→ 承認済みドメイン

次が必要です。

```text
hassi1020-tech.github.io
sample1-8011f.firebaseapp.com
```

## 6. APIキーについて

`firebase-config.js`には現在のFirebase Webアプリ設定を入れています。

まだ `API key not valid` が出る場合は、
Firebase Consoleの

プロジェクト設定
→ 全般
→ マイアプリ
→ SDKの設定と構成
→ Config

から `apiKey` をコピーし、
`firebase-config.js` のapiKeyへ貼り直してください。

## 7. エラー表示

Ver.13.1では、ログイン失敗時に画面へ
Firebaseのエラーコードと確認箇所を表示します。

例:

- auth/unauthorized-domain
- auth/operation-not-allowed
- auth/invalid-api-key
- auth/internal-error
