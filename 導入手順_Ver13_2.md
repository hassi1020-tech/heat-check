# Ver.13.2 ログイン状態取得修正版

Ver.13.1を維持したまま、Googleログイン後にアプリが「未ログイン」のままになる問題を修正した版です。

## 修正内容

- Firebase Consoleから取得した正しいAPIキーを組み込み
- Googleログインをポップアップ方式へ戻して安定化
- ログイン成功直後にFirebase Userを明示的に取得
- `onAuthStateChanged`でログイン状態を再確認
- ログイン成功時にユーザー名またはメールアドレスを表示
- Firestore同期開始処理をログイン成功後に実行
- PWAキャッシュ番号を更新

## GitHubへの反映

ZIPを解凍し、GitHubの`heat-check`直下へ全ファイルを上書きしてください。

最低限、次のファイルは必ず上書きします。

```text
firebase-config.js
firebase-cloud.js
index.html
sw.js
manifest.webmanifest
app.js
```

## 反映後のURL

```text
https://hassi1020-tech.github.io/heat-check/?mode=admin&v=1320authstate
```

## 古いキャッシュの削除

1. アプリを開く
2. F12
3. Application
4. Storage
5. Clear site data
6. ブラウザを閉じる
7. 上記URLを開く

簡易的には`Ctrl + Shift + R`でも更新できます。

## 正常時の表示

Googleログイン後、画面上部が次のように変わります。

```text
Firebase同期　同期済み
○○さんとしてログイン中です。
```

「未ログイン」のままなら、F12のConsoleに出る赤いエラーを確認してください。
